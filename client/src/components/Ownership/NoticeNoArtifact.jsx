export const NoticeNoArtifact = () => {
    return (
      <p>
        ⚠️ Cannot find <span className="code">WaterNFT</span> contract artifact.
        Please complete the above preparation first, then restart the react dev server.
      </p>
    );
}